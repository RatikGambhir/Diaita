# Base URL of the diaita-service Ktor backend.
NUXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Supabase project credentials (anon/publishable key — safe to expose to the browser).
NUXT_PUBLIC_SUPABASE_URL=
NUXT_PUBLIC_SUPABASE_KEY=
