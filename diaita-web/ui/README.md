# Diaita web

Nuxt/Vue client for the Diaita API.

```bash
npm install
npm run dev
```

The app expects the API at `http://localhost:8080`. Set `NUXT_PUBLIC_API_URL` to use another base URL. Run `npm run build` for a production build.
